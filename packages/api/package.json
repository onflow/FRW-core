{
    "name": "@onflow/frw-api",
    "version": "0.0.1",
    "description": "Package for flow wallet network layer with backend server api",
    "type": "module",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "files": [
        "src",
        "dist"
    ],
    "scripts": {
        "build": "tsup",
        "build:watch": "tsup --watch",
        "type-check": "tsc --noEmit",
        "clean": "rm -rf dist",
        "test": "vitest",
        "test:run": "vitest --run",
        "test:coverage": "vitest --coverage",
        "prepublishOnly": "pnpm build",
        "codegen": "node scripts/codegen.cjs && node scripts/codegen-go.cjs",
        "lint": "eslint . --cache",
        "lint:fix": "eslint . --cache --fix"
    },
    "exports": {
        ".": {
            "types": "./src/index.ts",
            "default": "./src/index.ts"
        },
        "./*": {
            "types": "./src/*.ts",
            "default": "./src/*.ts"
        }
    },
    "dependencies": {
        "swagger-axios-codegen": "^0.17.4",
        "axios": "^1.4.0"
    }
}
